RewriteEngine On

# Rewrite "/quiz" to "/index.html"
RewriteRule ^quiz$ /index.html [L]

# Rewrite URLs to HTML files if they exist
RewriteCond %{REQUEST_FILENAME} !-d
RewriteCond %{REQUEST_FILENAME}\.html -f
RewriteRule ^(.*)$ $1.html [L]